<%= form_for(@social_module) do |f| %>
  <% if @social_module.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@social_module.errors.count, "error") %> prohibited this social_module from being saved:</h2>
      <ul>
      <% @social_module.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div id="links">
    <%= f.fields_for :links do |link| %> 
      <%= render 'link_fields', f: link %> 
    <% end %> 
    <div class="links">
      <%= link_to_add_association 'add link', f, :links %> 
    </div>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
